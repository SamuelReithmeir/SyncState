<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>

        <!-- Package-specific metadata -->
        <PackageId>SyncState.Diagnostics</PackageId>
        <Description>Diagnostic tools and extensions for SyncState.</Description>
        <PackageTags>$(PackageTags);diagnostics;</PackageTags>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\SyncState.Core\SyncState.Core.csproj" />
    </ItemGroup>

</Project>
